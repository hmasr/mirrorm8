import { state } from "~/store/weather/state";
import { actions } from "~/store/weather/actions";
import { getters } from "~/store/weather/getters";
import { mutations } from "~/store/weather/mutations";
import { Module } from "vuex";
import { WeatherState, RootState } from "~/types";

const namespaced: boolean = true;

export const profile: Module<WeatherState, RootState> = {
  namespaced,
  state,
  getters,
  actions,
  mutations
};
